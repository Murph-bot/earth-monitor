# packages/shared

Types and tokens shared between web and mobile:

- **Typed API client** — generated from the backend's OpenAPI spec (Phase 6).
  Generated, not hand-written: the spec is the single source of truth.
- **Design tokens** — colors/spacing consumed by web CSS and future mobile
  theme (Phase 7).
