{
  "name": "earth-monitor",
  "private": true,
  "workspaces": [
    "web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "gen:client": "cd backend && uv run python -m scripts.dump_openapi && cd ../packages/shared && npm run gen"
  }
}
