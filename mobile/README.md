# mobile

Placeholder — Phase 9 decides the path (Expo/React Native vs Flutter vs PWA-only).

When built: same `/v1` API, same generated client from `packages/shared`,
same alert engine (mobile push becomes just another notification channel).
