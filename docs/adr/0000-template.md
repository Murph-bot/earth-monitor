# {Short title of the decision}

Status: proposed | accepted | deprecated | superseded by ADR-NNNN (date, phase)

{1–3 sentences: what's the context, what did we decide, and why.}

## Considered options (optional)

Only when rejected alternatives are worth remembering.

## Consequences (optional)

Only when non-obvious downstream effects need calling out.

---
An ADR can be a single paragraph. Record _that_ a decision was made and _why_ —
skip sections that add nothing. Worth writing when the decision is hard to
reverse, surprising without context, and the result of a real trade-off.
